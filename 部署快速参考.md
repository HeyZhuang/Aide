# 🚀 部署快速参考

## 一键部署（推荐）
```bash
cd /home/ubuntu/cckz/psd-canvas-jaaz
./quick-deploy.sh
```

## 手动部署三步走

### 1️⃣ 更新代码
```bash
cd /home/ubuntu/cckz/psd-canvas-jaaz
git pull origin main
```

### 2️⃣ 运行部署脚本
```bash
./deploy.sh
```

### 3️⃣ 重启服务
```bash
sudo systemctl restart psd-backend
sudo systemctl restart psd-frontend
```

---

## 📊 常用命令

### 服务管理
```bash
# 启动服务
sudo systemctl start psd-backend
sudo systemctl start psd-frontend

# 停止服务
sudo systemctl stop psd-backend
sudo systemctl stop psd-frontend

# 重启服务
sudo systemctl restart psd-backend
sudo systemctl restart psd-frontend

# 查看状态
sudo systemctl status psd-backend
sudo systemctl status psd-frontend
```

### 查看日志
```bash
# 实时日志
sudo journalctl -u psd-backend -f
sudo journalctl -u psd-frontend -f

# 最近50条日志
sudo journalctl -u psd-backend -n 50
sudo journalctl -u psd-frontend -n 50
```

### 检查端口
```bash
sudo netstat -tulpn | grep -E "(3004|57988)"
```

### 测试API
```bash
curl http://127.0.0.1:57988/api/health
curl http://127.0.0.1:3004
```

---

## 🔧 故障排查

### 服务无法启动
```bash
# 查看详细日志
sudo journalctl -u psd-backend -n 100
sudo journalctl -u psd-frontend -n 100
```

### 端口被占用
```bash
sudo lsof -i :3004
sudo lsof -i :57988
```

### 重新部署
```bash
cd /home/ubuntu/cckz/psd-canvas-jaaz
./deploy.sh
sudo systemctl restart psd-backend psd-frontend
```

---

## 📍 关键信息

- **项目目录**: `/home/ubuntu/cckz/psd-canvas-jaaz`
- **后端端口**: 57988
- **前端端口**: 3004
- **服务器IP**: 54.189.143.120
- **访问地址**: http://54.189.143.120/

---

**详细文档**: 查看 `部署流程完整指南.md`

